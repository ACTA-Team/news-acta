import {
  generateMonthlyReviewMetadata,
  MonthlyReviewIndexPageContent,
} from '@/components/modules/monthly-review';

export const generateMetadata = generateMonthlyReviewMetadata;

export default MonthlyReviewIndexPageContent;
