/**
 * Public layout API — import from `@/layouts` only.
 *
 * - `shell/` — site chrome (header, footer)
 * - `content/` — page width + long-form typography primitives
 */
export { SiteFooter, SiteHeader } from './shell';
export { Container, Prose } from './content';
